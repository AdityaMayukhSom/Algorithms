import java.util.ArrayList;

public class FloydCycleDetectionAlgorithm {

    // Finding Duplicate Value In An Array
    // This algorithms is actually that of an linked list, but we have to modify it.
    // Given an array of integers nums containing n + 1 integers where each integer
    // is in the range [1, n] inclusive.
    // There is only one repeated number in nums, return this repeated number.
    // You must solve the problem without modifying the array nums and uses only
    // constant extra space.
    public int findDuplicate(int[] nums) {

        return -1;
    }
}

class LinkedList {
    private ArrayList<Node> linkedList = new ArrayList<Node>();

}

class Node {
    private int num;
    private Node next;

}